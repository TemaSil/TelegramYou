*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
out/
app/release/
app/build/
core/build/
*.jks
*.keystore
# ...except the debug one, which is deliberately tracked. See CLAUDE.md:
# it is not a secret, and CI regenerating it per run is what made every
# published APK refuse to install over the last.
!app/debug.keystore
secrets.properties
third_party/
/tmp-tdlib/

# Large native TDLib libs (keep locally / CI cache)
app/src/main/jniLibs/

